# App login credentials (protect the settings/dashboard)
APP_USERNAME=admin
APP_PASSWORD=changeme

# Session secret – use a long random string in production
SESSION_SECRET=change-this-to-a-random-secret

# Microsoft Azure AD – filled in via the Settings page or directly here
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Server port
PORT=3000
